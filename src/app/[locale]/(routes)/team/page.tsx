import React from "react";
import TeamHeader from "./TeamHeader";
import TeamContent from "./TeamContent";

const page = () => {
  return (
    <div>
      <TeamHeader />
      <TeamContent />
    </div>
  );
};

export default page;
